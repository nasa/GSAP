language: cpp

# Use reasonable compiler versions
matrix:
  include:
    - os: linux
      compiler: gcc
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-7
      env:
        - MATRIX_EVAL="CC=gcc-7 && CXX=g++-7"
    - os: linux
      compiler: clang
      addons:
        apt:
          sources:
            - llvm-toolchain-trusty-5.0
          packages:
            - clang-5.0
      env:
        - MATRIX_EVAL="CC=clang-5.0 && CXX=clang++-5.0"
    - os: osx
      compiler: clang
      env: MATRIX_EVAL=""
    - os: osx
      env: MATRIX_EVAL="GENERATOR='-G Xcode'"

before_install:
    - eval "${MATRIX_EVAL}"

script:
  - set OPEN_MP_NUM_THREADS 4
  - mkdir build
  - mkdir bin
  - mkdir -p bin/testresults
  - pushd build
  - cmake .. $GENERATOR
  - cmake --build .
  - popd
  - pushd bin
  - ./gsapTests
  - ./commCollectionTests

